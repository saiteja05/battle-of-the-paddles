# Local file store (default) — no Docker or Mongo required for event night
STORE=file

# Shared operator PIN for setup + board operators
OPERATOR_PIN=0909

# Optional: MongoDB Atlas for multi-device sync in production
# MONGODB_URI=mongodb+srv://user:pass@cluster.example/paddles
# STORE=mongo

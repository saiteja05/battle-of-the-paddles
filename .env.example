# MongoDB (preferred). If unreachable, the app falls back to data/tournament.json
MONGODB_URI=mongodb://127.0.0.1:27017/paddles

# Shared operator PIN for the app operator + physical-board marker
OPERATOR_PIN=0909

# Force JSON file store even if Mongo is up: file | mongo | auto
STORE=auto
